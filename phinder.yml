- id: realworld.security.oauthToken
  pattern: "(_)->oauthToken()"
  message: |
    OAuth token is a credential
    Make sure not to expose the value to users or logs.
    Use '->filteredOAuthToken()' for obfuscated token.
